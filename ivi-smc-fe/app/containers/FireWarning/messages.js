/*
 * RentalOfficeManagement Messages
 *
 * This contains all the text for the RentalOfficeManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RentalOfficeManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RentalOfficeManagement container!',
  },
});
