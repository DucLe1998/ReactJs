// import { selectRentalOfficeManagementDomain } from '../selectors';

describe('selectRentalOfficeManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
