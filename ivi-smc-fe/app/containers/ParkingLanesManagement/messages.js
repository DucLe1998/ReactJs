/*
 * MapIndoorManagement Messages
 *
 * This contains all the text for the MapIndoorManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MapIndoorManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapIndoorManagement container!',
  },
});
