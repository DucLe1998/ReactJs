import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.newsManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Page_not_found',
  },
});
