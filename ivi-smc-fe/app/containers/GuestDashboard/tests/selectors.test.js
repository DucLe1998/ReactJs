// import { selectGuestDashboardDomain } from '../selectors';

describe('selectGuestDashboardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
