/*
 * GuestDashboard Messages
 *
 * This contains all the text for the GuestDashboard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GuestDashboard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GuestDashboard container!',
  },
});
