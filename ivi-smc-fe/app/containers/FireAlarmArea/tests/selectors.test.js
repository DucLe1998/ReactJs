// import { selectFireAlarmAreaDomain } from '../selectors';

describe('selectFireAlarmAreaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
