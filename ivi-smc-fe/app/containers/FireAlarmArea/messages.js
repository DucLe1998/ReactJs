/*
 * FireAlarmArea Messages
 *
 * This contains all the text for the FireAlarmArea container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FireAlarmArea';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FireAlarmArea container!',
  },
});
