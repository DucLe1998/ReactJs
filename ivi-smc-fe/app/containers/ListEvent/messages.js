/*
 * ListEvent Messages
 *
 * This contains all the text for the ListEvent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListEvent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListEvent container!',
  },
});
