// import { selectMapIndoorManagementDomain } from '../selectors';

describe('selectMapIndoorManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
