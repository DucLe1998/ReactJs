/*
 * ForbiddenArea Messages
 *
 * This contains all the text for the ForbiddenArea container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ForbiddenArea';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ForbiddenArea container!',
  },
});
