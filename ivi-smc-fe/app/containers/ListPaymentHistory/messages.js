/*
 * ListPaymentHistory Messages
 *
 * This contains all the text for the ListPaymentHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListPaymentHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListPaymentHistory container!',
  },
});
