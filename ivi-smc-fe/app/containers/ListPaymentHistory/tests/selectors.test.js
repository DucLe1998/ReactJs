// import { selectListPaymentHistoryDomain } from '../selectors';

describe('selectListPaymentHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
