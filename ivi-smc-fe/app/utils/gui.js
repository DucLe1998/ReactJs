const gui = {
  screenWidth: window.innerWidth,
  screenHeight: window.innerHeight,
  screenWidthOuter: window.outerWidth,
  screenHeightOuter: window.outerHeight,
  heightTable: window.innerHeight - 250,
  heightContentOfScreen: window.innerHeight - 155,
  optionsPageSize: [25, 50, 100],
  optionsPageIndex: 1,
  mainColor: '#272e56',
};

export default gui;
